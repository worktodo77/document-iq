"""Stage 6 — the post-run self-check (§4, §7).

Two gates live here: the corpus-wide page-accounting reconciliation, and the
output hash manifest that makes the byte-identical claim checkable rather than
asserted.
"""

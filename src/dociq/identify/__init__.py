"""Stage 3 — identity read off the page itself (Bates stamps).

Nothing in here infers meaning. A Bates number is a mechanical mark applied to
a page during production; detecting it is closer to reading a page number than
to interpreting content, which is what keeps it inside DocIQ's "reducer, not
interpreter" boundary (§1).
"""
